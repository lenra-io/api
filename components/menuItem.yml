# FILEPATH: /home/taorepoara/lenra/api/components/menuItem.yml
$schema: https://raw.githubusercontent.com/lenra-io/ex_component_schema/beta/priv/static/draft-lenra.json
$id: components/menu_item.yml
title: MenuItem
description: Element of type MenuItem
type: object
properties:
    _type:
        description: The type of the element
        const: menuItem
    text:
        description: The text of the element
        type: string
    isSelected:
        description: Whether the element is selected or not.
        type: boolean
        default: false
    disabled:
        description: Whether the element should be disabled or not.
        type: boolean
        default: false
    icon:
        $ref: ../components/icon.yml
    onPressed:
        $ref: ../defs/listener.yml
required:
    - type
    - text
additionalProperties: false
